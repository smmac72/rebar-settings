// todo

import { useTranslate } from '@Shared/translate.js';
const { setBulk } = useTranslate();

setBulk({
    en: {
        'settings.font_size': 'Font Size',
        'settings.show_timestamp': 'Show Timestamp',
        'settings.default': 'Reset to default',
        'settings.close': 'Close',
    },
});